influxdb {
  server-url      = "http://localhost:8086"
  org-name        = "homedata"
  project-bucket  = "home_data"
  time-resolution = 15 minutes
  batch-writes    = 250
}

octopus {
  requests-per-sec = 4
  page-size        = 100
}

tado {
  requests-per-sec = 12
}

scrape-log {
  log-path   = ~/.homedata/scrape.log
  batch-size = 100
  batch-wait = 250 milliseconds
}
